// Modules
// Every file in a node is a module by default.
const names = require('./names');
const sayHi = require('./func');
const data = require('./alternative-flavor');
require('./mind-grenade');
// console.log(data);
// sayHi('susan');
// sayHi(names.john);
// sayHi(names.peter);